tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "Test Report" tasks.robot

  Ping Test:
    robotTaskName: Ping

  Reboot Test:
    robotTaskName: Reboot

condaConfigFile: conda.yaml

environmentConfigs:
  - conda.yaml

artifactsDir: output

PATH:
  - ..
PYTHONPATH:
  - ..

ignoreFiles:
  - ../.gitignore
